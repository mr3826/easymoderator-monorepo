'use strict';

/**
 * Integration Features Router
 *
 * Inventory-sync subsystem removed (2026-05-20).
 * This file is retained as the module entry point.
 * Future integration endpoints (e.g. Shopify webhook receiver) should be added here.
 */

const { Router } = require('express');

const router = Router();

module.exports = router;
