# 03 — Secret Changes

**Names, scopes, and statuses only. No secret value or hash of a value is recorded.**

| Secret | Scope | Status | UTC timestamp | Reason |
|---|---|---|---|---|
| `CSRF_SECRET` | repository (actions) | `CREATED` | 2026-07-26T10:10:33Z | Category B, application-owned. Required by the production config validator; was missing and blocked the deploy. Generated with `openssl rand -hex 32` (32 bytes entropy) and piped to `gh secret set CSRF_SECRET` over stdin — value never printed, never written to a file, never placed in argv. |
| `PAYMENT_ENCRYPTION_KEY` | repository (actions) | `UNCHANGED` | — | Category D. Left exactly as-is in GitHub. Compatibility is achieved by render-time normalization (see `04_PAYMENT_KEY_COMPATIBILITY.md`), not by rotating the secret. |
| `SENTRY_DSN` | (backend) | `BLOCKED_EXTERNAL_CREDENTIAL` | — | Cannot be generated. The workflow now falls back to the existing `VITE_SENTRY_DSN` so the backend still has a sink; a dedicated backend DSN remains a founder action. |
| `SLACK_ALERT_WEBHOOK_URL` | (backend) | `BLOCKED_EXTERNAL_CREDENTIAL` | — | External provider URL; not generated. Optional second sink. |
| `BKASH_APP_KEY`, `BKASH_APP_SECRET`, `BKASH_BASE_URL`, `BKASH_USERNAME`, `BKASH_PASSWORD` | bKash | `NOT_REQUIRED_WHILE_DISABLED` | — | External merchant credentials. bKash is disabled for the pilot; the validator/render no longer require them. Never fabricated. |
| `BKASH_WEBHOOK_SECRET` | bKash webhook | `NOT_REQUIRED_WHILE_DISABLED` | — | Moved out of `CORE_REQUIRED`; required only when `BKASH_ENABLED=true`. Not fabricated for a disabled integration. |
| `SPACES_ACCESS_KEY_ID`, `SPACES_SECRET_ACCESS_KEY`, `SPACES_ENDPOINT`, `BACKUP_BUCKET` | backup off-site | `BLOCKED_EXTERNAL_CREDENTIAL` | — | Object-storage credentials must come from the founder's DO Spaces / S3 account. The upload step is implemented and guarded. |
| `BACKUP_ENCRYPTION_KEY` | backup off-site | `BLOCKED_EXTERNAL_CREDENTIAL` (founder-generated) | — | Deliberately **not** generated by the agent: a backup passphrase set as a GitHub secret cannot be read back, so whoever creates it must preserve a copy off-platform to ever decrypt a restore. The founder generates it (`openssl rand -hex 32`), stores it in a password manager, then sets it. |

## Policy adherence

- No fake bKash / Meta / Sentry / Slack / DigitalOcean / courier credential was created.
- No existing secret was replaced, copied between scopes, or read.
- The only secret created is the application-owned `CSRF_SECRET`.
