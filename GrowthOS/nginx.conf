server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location = /health {
        default_type application/json;
        return 200 '{"status":"healthy","service":"growth-os-frontend"}';
    }

    location = /health/ready {
        default_type application/json;
        return 200 '{"status":"ready","service":"growth-os-frontend"}';
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
